import 'package:flutter/material.dart';
import 'tabletmode.dart';

class TudaRate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'TUDARate',
      home: TabletMode(),
    );
  }
}
